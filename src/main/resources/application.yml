spring:
  datasource:
    url: jdbc:mysql://${MYSQL_HOST:localhost}:3306/voteMe?useSSL=false
    username: shazzar
    password: Las-jefa5
  jpa:
    hibernate:
      ddl-auto: update

server:
  error:
    include-stacktrace: never
    include-message: always